language: node_js
node_js: stable
branches:
  only:
  - master
install:
- npm install
script:
- npm run docs:build
after_success:
  - cd docs/.vuepress/dist
  - git init
  - git config --global user.name 'jolylai'
  - git config --global user.email '2635850288@qq.com'
  - git add .
  - git commit -m "generate static resources, triggerd by travis ci"
  - git push --force "https://jolylai:${REPO_TOKEN}@${GH_REF}" master:gh-pages
env:
  global:
    - GH_REF: github.com/jolylai/jolylai.github.com.git
