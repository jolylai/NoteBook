language: node_js
node_js: stable
install:
- npm install
script:
- npm run docs:build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $REPO_TOKEN
  keep-history: true
  on:
    branch: master
